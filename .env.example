# Purpose-specific, read-only SigNoz service-account key sent by the MCP client.
# Never commit the populated value.
SIGNOZ_API_KEY=

# Restricted Neon role for the Verifier. Never commit the populated value.
VERIFIER_DATABASE_URL=

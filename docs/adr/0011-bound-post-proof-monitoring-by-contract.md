# Bound post-proof monitoring by contract

Each Completion Contract will define a Monitoring Window during which Receipt continues checking a `PROVEN` Claim for later changes that could produce `REVERSED`. When the window closes, Receipt keeps the latest verdict and shows when monitoring ended rather than implying the Claim is watched forever; production windows may vary by Claim Type while demos may use compressed timing.

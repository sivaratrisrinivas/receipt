# Developers own Completion Contracts

Developers will define and version Completion Contracts; an AI may map an agent's words to a supported Claim Type, but it cannot invent the required evidence or decide the Verification Verdict. Receipt will return `INCONCLUSIVE` when it cannot map a Claim to a supported contract, trading unrestricted claim coverage for deterministic, testable proof.
